export const i18n = {
  defaultLocale: 'en',
  // don't forget to import new locale or remove old one in DayJSLocale component
  locales: ['en', 'ru'],
} as const;

export const PREFERRED_LOCALE_COOKIE = 'preferred-locale';
